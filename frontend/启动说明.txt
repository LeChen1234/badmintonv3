启动前端步骤
=============

1. 安装 Node.js（LTS），并勾选 `Add to PATH`

2. 在项目根目录执行：
   - 双击 `run_frontend.bat`
   - 或命令行：
     - `cd frontend`
     - `npm install`
     - `npm run dev`

3. 浏览器访问：
   - `http://localhost:3000`

若提示 `npm 不是内部或外部命令`，请重新安装 Node.js 或手动把 Node 安装目录加入 PATH。
